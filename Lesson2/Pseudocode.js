/*
A function that returns the sum of two numbers
Start
Set function with arguments summand1 and summand2
  Add the two numbers together
  Print the sum
End
*/

/*
A function that takes an array of strings, and returns a string that is all those strings concatenated together

Start
Set function with one argument that is expected to be an array.
  Set result variable
  Use a method to concat all elements ot the array and save it to the result variable
  Print the result variable
End
*/

/*
A function that takes an array of integeres, and returns a new array with every other element

Start
Set function with one argument that is expected to be an array
  Set empty result array
  Set iterator
  While iterator < input array length
    if current element fits a criteria
      add the element to the result array
  print result array
End